

## Git
Always use conventional commits, with scope if appropriate.
Avoid diffing before committing and git patches, which usually fail.

## Vue
Always prefer composition API + `<script setup lang="ts">`.
Prefer `@vueuse/core` instead of writing custom utilities.